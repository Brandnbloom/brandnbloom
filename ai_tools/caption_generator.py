import os
from ai_tools.prompts import CAPTION_PROMPT
import requests

def generate_caption(context: str) -> str:
    # Placeholder deterministic output to avoid external calls
    return f"Unlock your growth with AI 🌸 {context} — tap to save & share!"

